import React from 'react';
import Layout from '../components/Layout';

interface IProps {

}

const Talks: React.FC<IProps> = (props) => {
	return (
		<Layout>
			Talks
		</Layout>
	);
}

export default Talks;